# TODO
- Add login with passeport
- Add CI with travis
- Add stamp programm

# Deploy to heroku
```bash
git push heroku master
```

# Local development

## Backend
- `yarn` && `yarn dev`


## Frontend
- `cd ./react-ui` && `yarn` && `yarn start`